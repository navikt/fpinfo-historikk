spring:
   cloud:
      vault:
         database:
              backend: postgresql/preprod-fss
         kubernetes:
              kubernetes-path: kubernetes/preprod/fss
         generic:
             backend: kv/preprod/fss